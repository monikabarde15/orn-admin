# docker-compose.prod.yml  (pure production, no volumes, no dev dependencies)
services:
    web:
        image: node:22-alpine
        working_dir: /app
        command: >
            sh -c "
              npm ci --omit=dev &&
              npm run build &&
              npx serve -s dist -l 3000
            "
        ports:
            - '3000:3000'
        environment:
            - NODE_ENV=production
        # No volumes → container is immutable and secure
