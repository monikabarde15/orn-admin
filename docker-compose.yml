services:
    web:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: react_app
        command: serve -s dist -l 3000
        ports:
            - '3000:3000'
        volumes:
            - .:/app
        env_file:
            - .env
