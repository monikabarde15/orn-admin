services:
    web:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: react_app
        command: npm run preview
        ports:
            - '3000:4173'
        volumes:
            - .:/app
        env_file:
            - .env
