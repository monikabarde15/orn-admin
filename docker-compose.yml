services:
    web:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: react_app
        command: >
            sh -c "
                npm install &&
                npm run preview
            "
        ports:
            - '4173:4173'
        volumes:
            - .:/app
        env_file:
            - .env
